# 🎨 بازطراحی UI افزونه‌ها — فاز ۴

هاب (v1.2.0) زبان طراحی را جا انداخت: توکن‌ها، `tisacase-ui.css`، ردیفِ لانچر، و تزریق استایل روی صفحه‌های افزونه‌ها. این فایل برنامهٔ **تک‌به‌تک** بازطراحی خودِ ۹ افزونه است. عددِ هر ستون از شمارش روی کدِ واقعی زیپ‌ها به‌دست آمده (`/tmp` شمارش شد، منبع: زیپ‌های ریشه).

## قانون بازی (برای هر ۹ افزونه یکی)

1. **منطق دست‌نخورد.** فقط ظاهر: CSS، مارکاپِ رندر، کلاس‌ها، چیدمان، متن‌های رابط. هک، فیلتر، کوئری، `nonce`، `option`، جدول دیتابیس، AJAX endpoint و shortcodes همان‌ها می‌مانند.
2. **منبعِ کد = `plugins/src/<dir>/`** که با `tools/extract-plugin.sh <dir>` از زیپ ریشه ساخته می‌شود. زیپ‌های ریشه **هیچ‌وقت** ویرایش نمی‌شوند؛ خروجی ما `plugins/dist/<dir>.zip` است.
3. **رنگ‌ها فقط از توکن.** هر `#hex` و `border-radius: 7px` به `var(--tisa-primary)`/`var(--tisa-r-sm)`/… تبدیل می‌شود. اگر رنگی در توکن نبود، در همان PR به `design/tisacase-ui.css` اضافه می‌شود (نه هاردکد در افزونه).
4. **قلم:** حذف هر `fonts.googleapis.com` / CDN (یافتهٔ F5 در `case-special-package`)؛ قلم از هاب می‌آید، با fallback سیستمی.
5. **قرارداد اعداد §۸:** مبلغ و شمارنده با قلم رابط + `tabular-nums`؛ مونو **فقط** برای کد (SKU، شناسه، نام فایل، لاگ). این را در v0.1 اشتباه کردیم و ارقام فارسیِ ستون قیمت شکست.
6. **هاب را دو‌بار‌کاره نکن.** دکمه، نوتیس، جدول، فرم، تب، کارت، input — همه از `.tisa-*` می‌آیند؛ افزونه فقط کلاس می‌زند و در بدنه `tisa-scope` قرار می‌گیرد. اگر کامپوننتی کم بود، به `design/tisacase-ui.css` اضافه می‌شود (منبع واحد).
7. **بدون وابستگی جدید:** بدون build step، بدون framework، بدون فایل `.scss`؛ همان CSS ساده که روی هاست اشتراکی کار کند.
8. **هر افزونه = یک کامیت + یک زیپ + یک چک‌لیست.** اگر جایی شکست، همان افزونه را برمی‌گردانیم؛ بقیه دست‌نخورده.

## ترتیب پیشنهادی (از کوچک به بزرگ — اول الگو را روی یک فایلِ کم‌ریسک می‌سازیم)

| # | افزونه | حجم کار | آنچه امروز هست | هدف |
|---|---|---|---|---|
| ۱ | `wc-sku-prefix-bar` | خیلی کم | `assets/bar.css` · ۱۵ hex · ۲ `radius` هاردکد · ۱ inline | نوار SKU روی لیست محصولات: ارتفاع/گوشه/رنگ از توکن؛ حالت کم‌فضا؛ سازگاری با ستون‌های ووکامرس |
| ۲ | `tisacase-order-phone-exporter` | کم | بدون فایل CSS · ۸ hex · **۱۳ `style="`** در PHP | همهٔ استایل‌های inline → کلاس؛ فرم خروجی و جدول پیش‌نمایش با `.tisa-form/.tisa-table` |
| ۳ | `tisa-product-importer` | کم‌متوسط | بدون فایل CSS · ۸ hex · ۸ inline | صفحهٔ آپلود + لاگ پیشرفت: `.tisa-codebox` برای لاگ، `.tisa-progress`، کارت نتیجه |
| ۴ | `tisacase-pricing-manager` | متوسط | ۱ فایل CSS · ۱۴ hex · ۲ radius | جدول قانون‌ها: `.tisa-table` + سطرهای متناوب + ستون‌های عددی با قرارداد §۸ |
| ۵ | `case-special-package` | متوسط | ۱ CSS · **۶۴ hex** · **۲۱ radius** · **۱ فایل CDN قلم** | حذف `fonts.googleapis.com` (F5)، توکنی‌شدن تنظیمات و فرم استثناها |
| ۶ | `tisacase-product-description` | زیاد | ۱ CSS · ۵۸ hex · ۱۳ radius | سه تب (تنظیمات/ابزارها/بازگردانی) → کامپوننت تب مشترک؛ جدول اسکن و بکاپ |
| ۷ | `tisacase-bulk-price-manager` | زیاد | ۱ CSS · ۳۹ hex · **۲۲ inline** · ۶ radius | جدول بزرگ ویرایش قیمت + پیش‌نمایش + لاگ اجراها: `sticky header`، ستون مبلغ با `.tisa-money`، اعداد فارسی |
| ۸ | `bulk-product-cleaner` | زیاد | CSS به‌شکل رشته در `class-bdc-admin.php::css()` (خط ۷۲۹) · ۵۶ hex | `wp_add_inline_style` را نگه می‌داریم ولی متن CSS را به توکن تبدیل و در فایل `assets/admin.css` منتقل می‌کنیم (قابل کش شدن) |
| ۹ | `bulk-tracking-upload` | زیاد + حساس | `<style>` inline · ۳۳ hex · **۱۸ inline** | فقط UI. **F1 (نشت PII/IDOR)، F2 (ZIP)، F3 (bulk) باز نمی‌شوند** مگر با تأیید جدا — ولی اگر در همان صفحه بود، نشانهٔ «دسترسی» را به شکل `.tisa-badge` درست می‌کنیم |

## چرخهٔ کار برای هر افزونه

```bash
./tools/extract-plugin.sh tisacase-pricing-manager     # plugins/src/… را می‌سازد
# …بازطراحی: CSS/مارکاپ…
./tools/build-plugin-zip.sh tisacase-pricing-manager   # بررسی ایستا + plugins/dist/….zip
```

### چک‌لیست پذیرش (هر افزونه، قبل از زیپ)

```bash
python3 tools/ui-audit.py plugins/src/<dir>      # همهٔ چک‌های پایین، یک‌جا
```
`ui-audit` همان قانون‌ها را مکانیکی می‌کند: رنگ هاردکد (fallbackِ داخل `var(--x, #fff)` مجاز است — همان چیزی که با خاموش‌بودن هاب صفحه را نجات می‌دهد)، `border-radius`/`font-size` پیکسلی، ارجاع CDN، و `style="` تزئینی در PHP (مقدار پویا مثل `width: 42%` مجاز).

- [ ] `ui-audit` → همه ✓. با چشم چک نکن؛ هشت افزونه باقی است.
- [ ] `border-radius` / `font-size` / `padding` هاردکد → صفر (همه `var(--tisa-*)`).
- [ ] صفحه در حالت **کم‌فضا** (`body.tisa-compact`) و در **RTL** درست است؛ جدول سرریز افقی ندارد.
- [ ] مبالغ/درصدها با قلم رابط و `tabular-nums`؛ SKU/شناسه/لاگ با `.tisa-code`.
- [ ] نوتیس‌ها (`updated`/`notice`) به `.tisa-*` تبدیل شده‌اند و رنگ معنایی دارند (success/warning/danger/info).
- [ ] هیچ کلاس `.tisa-` جدیدی بی‌تعریف نمانده (اگر لازم شد، اول در `design/tisacase-ui.css`، بعد `./tools/build-hub-zip.sh`).
- [ ] با **هاب خاموش**، صفحه هنوز خواناست (fallback ها در `var()` و استایل پایهٔ افزونه).
- [ ] `python3 tools/php-check.py plugins/src/<dir>` صفر خطا · `node --check` برای JS‌های غیرمینی‌فای.
- [ ] تست روی هاست: باز کردن از لانچر هاب، ذخیرهٔ یک فرم، و یک بار `active/inactive`.

## وضعیت

| # | افزونه | حالت |
|---|---|---|
| ۱ | `wc-sku-prefix-bar` ۱٫۵٫۰ | ✅ بازطراحی شد → `plugins/dist/wc-sku-prefix-bar.zip` |
| ۲ | `tisacase-order-phone-exporter` | ⏳ بعدی (منتظر تأیید الگو) |
| ۳–۹ | بقیه | ⏸ |

**الگویی که روی ۱ تثبیت شد (ملاک هشت تای بعد):**
- هیچ کلاسِ `wcspb-*` عوض نشد → `bar.js` و `editor.js` صفر ویرایش؛ فقط CSS و مارکاپِ رندرشده.
- گرادیان هاردکد (`#064e46,#087c70,#129786`) که با رنگ برند هاب قفل نمی‌شد → پنل روشنِ `--tisa-primary-tint` با چیپ‌های فیروزه‌ای؛ حالا با تغییر رنگ برند در تنظیماتِ هاب، این نوار هم عوض می‌شود.
- رشته‌های انگلیسیِ رابط (`Checking…`, `Insert`, `Auto SKU`, `— choose a series —`) فارسی شدند.
- `style="display: inline-flex; visibility: visible; opacity: 1"` حذف شد؛ جایش قاعدهٔ CSS و دو قاعدهٔ `!important`ِ موجود (که با JSِ نجاتِ ووکامرس می‌جنگد) ماند.
- یک باگ UI رفع شد: `bar.js` روی `#wcspb-refresh` کلیک را می‌گرفت، ولی آن دکمه هیچ‌وقت رندر نمی‌شد — الان در ردیف هست و همان هندلرِ موجود را صدا می‌زند (بدون کد جدید).
- `role="toolbar"`، `role="status" aria-live="polite"` روی خط وضعیت، و `:focus-visible` با `--tisa-ring` اضافه شد.
- نسخهٔ استایل/اسکریپت از `const VER` می‌آید (قبلاً چهارجا `'1.4.0'` تکراری بود) و `deps` استایل، اگر هاب فعال باشد، `tisacase-ui` است تا کسکید توکن‌ها قطعی باشد.

## چیزی که در این فاز انجام **نمی‌دهیم**

- بازنویسی منطق `bulk-tracking-upload` (F1/F2/F3) — نیاز به تأیید صریح تو دارد.
- فاز ۳ (هدر `TisaCase Hub:` داخل هر افزونه + فیلتر `tisacase_hub_items`) — جدا و کوتاه است؛ پیشنهادم بعد از دو افزونه اول، وقتی registry هر دو طرف جا افتاد.
- کتابخانه/composer/`build step` — برای ۹ افزونهٔ زیپی روی هاست اشتراکی اضافه‌برندگی است (`DESIGN_PLAN.md` §۴).
