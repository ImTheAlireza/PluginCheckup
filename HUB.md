# 🧩 `tisacase-hub` — ساخته شد

**لینک مستقیم زیپ (نسخهٔ ۱٫۱٫۰):**
`https://raw.githubusercontent.com/ImTheAlireza/PluginCheckup/arena/01a09973-plugincheckup/tisacase-hub.zip`
صفحهٔ ریلیز: `https://github.com/ImTheAlireza/PluginCheckup/releases/tag/v1.1.0`


> نصب‌کردنی: `tisacase-hub.zip` (ریشهٔ مخزن) · منبع: `plugins/tisacase-hub/` · استایل مرجع: `design/tisacase-ui.css`
> شناخت افزونه‌ها: `PLUGIN_MAP.md` · تصمیم‌ها و قراردادها: `DESIGN_PLAN.md`

یک خط: هاب، **میزبان** زبان طراحی و تنها ورودی ۹ افزونهٔ اختصاصی است — خود افزونه‌ها دست‌نخورده می‌مانند.

---

## ۰) بازخورد شما روی نسخهٔ نصب‌شده → چیزی که در ۱٫۰٫۲ و ۱٫۱٫۰ درست شد

1. **کارت‌های اضافه** (۱٫۰٫۲، کامل در ۱٫۱٫۰): «افزونه‌ها»، «نمای کلی ووکامرس» و «سفارش‌ها» اصلاً افزونهٔ شما نیستند؛ از registry حذف شدند، پس هیچ‌وقت کارت نمی‌شوند. توضیح فنی‌اش در `DESIGN_PLAN.md` §۱۰.
2. **لانچر کم‌حرف** (۱٫۱٫۰): فقط *دیدن و باز کردن*. شمارنده‌ها، گزارش سلامت، نوار محیط (Woo/PHP/HPOS/قلم)، خط توضیح افزونه روی کارت و یادآور «ثبت‌نشده‌ها» از صفحهٔ لانچر بیرون رفتند. مدیریت همان ماند: فعال/غیرفعال‌سازی، سنجاق، مخفی‌کردن، رنگ برند، چگالی، جستجو.
3. **«اجازهٔ دسترسی نمیده»** (۱٫۱٫۰): علتش `remove_submenu_page()` بود — با حذف آیتم از `$submenu`، خودِ مسیر `admin.php?page=…` در هسته ۴۰۳ می‌شد. حالا مخفی‌سازی فقط با CSS است و هیچ مسیری بسته نمی‌شود. اگر نقشی غیر از مدیر دارید، `bdc` و `bulk-tracking-upload` (که `manage_options` می‌خواهند) دکمهٔ قفل‌شده با tooltip نشان می‌دهند، نه خطای هسته.

---

## ۱) نصب

```
پیشخوان › افزونه‌ها › افزودن افزونه › بارگذاری افزونه › tisacase-hub.zip › فعال‌سازی
```

یک گزینهٔ **مستقل** در نوار کنار وردپرس ظاهر می‌شود — با پیش‌فرض «بالا، درست بعد از داشبورد».
هاب هیچ ورودی‌ای داخل منوی ووکامرس ثبت نمی‌کند. جایگاه از
«ظاهر و تنظیمات › جایگاه هاب در نوار کنار» عوض می‌شود (بالای ووکامرس / بعد از محصولات /
قبل از ابزارها / انتهای فهرست / عدد دلخواه مثل `57.5`).
اگر `wp-cli` در دسترس است: `wp plugin install tisacase-hub.zip --activate`.

(اختیاری) قلم محلی: `vazirmatn-400.woff2` … `vazirmatn-800.woff2` را در
`wp-content/plugins/tisacase-hub/assets/fonts/` بگذارید. هاب خودش `@font-face` می‌سازد؛
هیچ فایلی نباید ویرایش شود. نبودشان هم چیزی را نمی‌شکند (قلم سیستم).

---

## ۲) دو صفحه‌ای که می‌گیرید

| صفحه | آدرس | کار |
|------|------|-----|
| **ابزارها (لانچر)** | `admin.php?page=tisacase-hub` | ۹ کارت: آیکون، عنوان، نسخه، وضعیت، [فعال‌سازی]/[باز کردن]، ✕ مخفی‌کردن، ★ سنجاق. همین. |
| **ظاهر و تنظیمات** | `admin.php?page=tisacase-hub-settings` | جایگاه منو، رنگ برند، چگالی، یکدست‌سازی صفحه‌های افزونه‌ها، مخفی‌کردن منوهای پراکنده، کارت‌های مخفی‌شده، راهنمای ثبت افزونهٔ جدید |

روی لانچر: `/**/` فیلتر · `↑↓` جابه‌جایی · `Enter` باز کردن در تب جدید · `Esc` پاک کردن · ★ سنجاق (در `user_meta` شما ذخیره می‌شود، نه cookie).

## ۳) «اجرا و مدیریت» دقیقاً یعنی چه

- **فقط افزونه‌های خودتان:** گرید لانچر ۹ کارت اختصاصی است و چیزی جز آن. ابزاری که صفحهٔ مستقل ندارد («نوار پیشوند SKU») به‌جای «باز کردن»، دکمهٔ «لیست محصولات» می‌گیرد — همان‌جا که نوار می‌نشیند.
- دامنهٔ یکدست‌سازی: صفحهٔ ویرایش محصول (`post.php` / `post-new.php`) عمداً خارج شد تا چیدمان جدول واریاسیون‌ها و پنل دادهٔ ووکامرس دست‌نخورده بماند؛ فقط لیست محصولات با کلید `style_product_screens` روشن/خاموش می‌شود. برای ویرایش محصول: `add_filter( 'tisacase_hub_skubar_screens', fn() => array( 'edit-product', 'product', 'add-product' ) );`


- **باز کردن** هر ابزار در تب جدید (`target="_blank" rel="noopener"`) — بدون iframe.
- **فعال/غیرفعال کردن** افزونه از همان کارت، با `confirm` برای غیرفعال‌سازی و پیام خطای وردپرس اگر فعال‌سازی نشد.
- **مخفی کردن کارت** (✕) و برگرداندنش از تنظیمات — فقط نمایش در لانچر را عوض می‌کند.
- **یکدست‌سازی زنده**: استایل هاب روی صفحه‌های خود افزونه‌ها تزریق می‌شود، پس همان‌جا هم دکمه/نوتیس/جدول/فرم‌ها به این زبان درمی‌آیند.
- **رنگ برند**: یک هگز در تنظیمات → توکن‌های `--tisa-primary*`، `soft/tint/ink/deep/bright/ring` از همان ساخته و با `wp_add_inline_style` روی همهٔ صفحه‌ها چاپ می‌شود.
## ۴) چطور کار می‌کند (۶ خط فنی)

```php
add_action( 'admin_enqueue_scripts', [ 'TSH_UI', 'inject' ], 1 );   // زودتر از CSS هر افزونه
add_filter( 'body_class',           [ 'TSH_UI', 'body_class' ], 20 ); // → .tisa-scope / .tisa-compact / .tisa-hub-page
// دامنه از registry می‌آید: هم screen id دقیق، هم پسوند _page_<slug> (مقاوم در برابر نام‌گذاری والد)
wp_enqueue_style( 'tisacase-ui', … 'assets/tisacase-ui.css' );
wp_add_inline_style( 'tisacase-ui', ':root{ --tisa-primary: … ; }' );
add_action( 'admin_print_styles', [ 'TSH_Admin', 'hide_scattered' ], 5 ); // فقط CSS؛ $menu/$submenu دست نمی‌خورد
```

- `uninstall.php` دارد (درس F4): با حذف هاب، فقط آپشن خودش و متای سنجاق‌ها می‌رود.
- با **غیرفعال‌کردن** هاب: استایل تزریقی می‌رود، منوها برمی‌گردند، صفحهٔ هیچ افزونه‌ای خراب نمی‌شود.
- لانچر هیچ کوئری‌ای دربارهٔ محتوا نمی‌زند: فقط `get_plugins()` + `is_plugin_active()` + `current_user_can()`.

## ۵) قرارداد اعداد (چیزی که در v0.1 شکست) در کد واقعی

- مبلغ → `TSH_View::num()` و کلاس `.tisa-money`؛ قلم رابط + `tabular-nums`؛ واحد پول جدا و muted.
- شمارنده/درصد → `.tisa-num` (+ `--lg`). مونو فقط برای **کد**: `.tisa-code` (SKU، شناسه، نام فایل، لاگ) — همان چیزی که ستون قیمت را خراب کرده بود.
- استایلِ تزریق‌شده روی جدول‌های افزونه‌ها هم همین قاعده را دارد: `input[type=number]` و ستون‌های `is-num` / `is-money` با قلم رابط، `td code` با مونو.

## ۶) چه چیزی هنوز انجام **نشده** (عمداً، تا تأیید شما)

1. **فاز ۳**: افزودن هدر `TisaCase Hub:` یا فیلتر `tisacase_hub_items` به خود ۹ افزونه. الان registry داخل هاب hardcode است (و کار می‌کند)؛ فاز ۳ فقط «ضدپوسیدگی» است — هر افزونه جدید بدون ویرایش هاب ثبت می‌شود.
2. **فاز ۴**: توکنی‌کردن ۴ فایل CSS اختصاصی (حذف هگزها و `radius`های هاردکد، حذف CDN قلم از `case-special-package`). تا آن موقع، آن صفحه‌ها با لایهٔ نرمال‌ساز یکدست به‌نظر می‌رسند ولی رنگ‌های خودشان را هم دارند.
3. **بازنویسی `bulk-tracking-upload`**: هاب در گزارش سلامت نشانش می‌دهد، ولی من بدون تأیید شما وارد منطق PII آن نمی‌شوم (F1/F2/F3).
4. تست روی سرور واقعی: این مخزن `php` ندارد، پس کد با `tools/php-check.py` (توازنویس/کوئری‌ساختار) و `node --check` بررسی ایستا شده؛ اجرا روی هاست شماست.

## ۷) بازتولید زیپ

```bash
./tools/build-hub-zip.sh     # CSS را از design/ کپی می‌کند، بررسی ایستا می‌گیرد، زیپ می‌سازد
python3 tools/php-check.py plugins/tisacase-hub
```

`design/tisacase-ui.css` مرجع است؛ `plugins/tisacase-hub/assets/tisacase-ui.css` نسخهٔ توزیعی — اسکریپت بالا هر دو را هم‌نگاه نگه می‌دارد.
